{
  "name": "notebackend",
  "version": "0.0.1",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:ui": "@powershell Remove-Item -Recurse -Force dist && cd ../part2-notes-frontend && npm run build && @powershell Copy-Item dist -Recurse ../part3-notes-backend",
    "deploy:full": "npm run build:ui && git add . && git commit -m uibuild && git push -u origin part3-1"
  },
  "author": "Matti Luukkainen",
  "license": "MIT",
  "description": "",
  "dependencies": {
    "express": "^5.1.0"
  }
}
